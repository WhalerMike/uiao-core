# NOTE: Documentation rendering has moved to uiao-docs.
# This Quarto config is retained only for potential local generation script testing.
# The canonical Quarto pipeline is at https://github.com/WhalerMike/uiao-docs/_quarto.yml

project:
  type: default
  output-dir: _output

format:
  html:
    theme: cosmo
    toc: true

mermaid:
  theme: neutral

execute:
  echo: false
  warning: false
