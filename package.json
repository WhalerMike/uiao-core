{
  "name": "uiao-core",
  "version": "0.1.0",
  "description": "High-performance utility modules for uiao-core task processing",
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "test": "vitest run",
    "bench": "vitest bench --reporter=verbose"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
