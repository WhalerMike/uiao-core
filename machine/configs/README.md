# machine/configs/

Machine-consumed configuration files for components, modules, and environments.

## Rules

- Only machine-parseable formats (`.json`, `.yaml`, `.yml`, `.toml`).
- No Markdown or prose.
- Environment-specific configs should be clearly labeled.
- Changes must be validated by CI before merge.
