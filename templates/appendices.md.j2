# Appendix Canon Library

This library contains the 104 standardized technical and operational patterns (A-CZ) used across the Modernization Atlas.

## Library Overview

```mermaid
{{ diagrams.appendix_library }}
```

## Table of Contents

{% set groups = [
  {'range': 'A', 'title': 'Core Standards (A-Z)'},
  {'range': 'AA', 'title': 'Network Patterns (AA-AZ)'},
  {'range': 'BA', 'title': 'Security Protocols (BA-BZ)'},
  {'range': 'CA', 'title': 'Operational Procedures (CA-CZ)'}
] %}

{% for group in groups %}
### {{ group.title }}

| ID | Title | Status |
|---|---|---|
{% for key, value in appendices.items() if key is defined %}
{% if key.startswith(group.range) %}
| {{ key }} | {{ value.title }} | {{ value.status | default('Draft') }} |
{% endif %}
{% endfor %}

{% endfor %}

---

### Implementation Notes

* **Logic Check:** The iteration logic assumes YAML keys start with the group prefix (e.g., `A-01`, `BA-02`).
* **Normalization:** Hyphen-to-underscore conversion is applied by `generate.py` before rendering.

---
*Generated by the UIAO-Core Pipeline*
