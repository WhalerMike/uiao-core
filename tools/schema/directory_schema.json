{
  "$comment": "<!-- NEW (Proposed) --> Directory Structure Schema",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "UIAO Directory Structure Schema",
  "description": "Schema for validating repository directory layout per Document 10.",
  "type": "object",
  "required": ["required_directories", "required_files"],
  "properties": {
    "required_directories": {
      "type": "array",
      "items": { "type": "string" },
      "description": "List of directories that must exist."
    },
    "required_files": {
      "type": "array",
      "items": { "type": "string" },
      "description": "List of files that must exist."
    },
    "forbidden_patterns": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Glob patterns for files/dirs that must not exist."
    }
  }
}
